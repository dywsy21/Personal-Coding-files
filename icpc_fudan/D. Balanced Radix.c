#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>

int main(){
    int n,i=0,a[16];
    scanf("%d",&n);
    while(n>=(int)pow(3,i)) i++;
    int det=((int)pow(3,i)-1)/2;

    if(n<=det){

    }
    else{







    }











    system("pause");
    return 0;
}