#include<bits/stdc++.h>

double findMedianSortedArrays(int* nums1, int nums1Size, int* nums2, int nums2Size){
    int num=nums1Size+nums2Size+10;
    int a[num];
    for(i=0;i<=num;i++){
        if(num1[i]==num2[i]) a[i]=num1[i];
        if(num1[i]<num2[i]){
            a[i]=num1[i]
        }
    }

}