#include<bits/stdc++.h>
using namespace std;

int main(){

    unordered_set<string> hashtable;
    vector<string> all_words;
    ....

    for(auto i:all_words) hashtable.insert(i);
    if(hashtable.find(?)!=hashtable.end())





    system("pause");
    return 0;
}