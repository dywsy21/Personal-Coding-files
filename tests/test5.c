#include<bits/stdc++.h>
using namespace std;
#define max(a,b) (a)>=(b)?(a):(b)
#define min(a,b) (a)<=(b)?(a):(b)

int main(){
    int a,b;
    cin >> a >> b;
    cout << max(a,b);
    system("pause");
    return 0;
}